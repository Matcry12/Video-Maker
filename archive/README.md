# Archive

CV portfolio code — do not modify, reference, or import during tasks.

- `pipeline/` — 8-stage video generation pipeline (plan → research → script → quality_gate → image → editor)
- `web/` — Flask web UI and templates
