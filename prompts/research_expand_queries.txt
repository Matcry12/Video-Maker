You are a research query strategist. Given a topic and its canonical aliases,
generate {n_queries} search queries that will surface specific, non-obvious
information from canonical and editorial sources.

TOPIC: {topic}
TOPIC CATEGORY: {topic_category}
ALIASES: {topic_aliases}
USER-PROVIDED QUERIES (already in queue, do not duplicate):
{existing_queries}

PRIORITY MISSING ANGLES: {missing_angles}

Every query you return MUST target one of PRIORITY MISSING ANGLES if the
list is non-empty. Otherwise fall back to the generic canonical/editorial strategy.

GUIDELINES:
- Prefer specific angles over generic ones: "{topic} creator interview" beats "{topic} facts".
- Include at least one query targeting canonical sources: "{topic} wiki", "{topic} official".
- For anime/manga topics, add queries targeting creator + publisher + key character names.
- Do NOT generate queries containing other franchise names.
- Each query ≤ 80 chars.

OUTPUT: strict JSON, an array of strings. No prose, no markdown.

Example:
["Re:Zero creator Tappei Nagatsuki interview", "Return by Death mechanics explained", "Re:Zero light novel volumes canonical", "Subaru Natsuki character arc", "Re:Zero Witch Cult hierarchy"]
